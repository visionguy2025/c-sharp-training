// Collections, Lists, Generic

// List<int> numbers = new List<int>();
// numbers.Add(10);
// numbers.Remove(10);

// <T> or <type> or <x> ... generic

// ArrayList - not recommended

// research - vs between c# object, dynamic, var

// LinkedList

// Dictionary
// Dictionary<int,string> /*(key value)*/ values = new Dictionary<int,string>();
// values.Add(10,"Salam"); values.Add(0, "A");

// Queue (generic, non-generic) // FiFo Enqueue Dequeue

// Stack // LiFo Push, Pop

// research List default capacity logic (2x increases), trimex for trim until count
// research using and namespace does same on executable or not
